paths:
  /api/v1/user:
    get:
      summary: Get a list of users
      security:
        - bearerAuth: []
      tags:
        - user
      responses: 
        '200':
          description: List of users
          content:
            application/json:
              schema: 
                type: array
                items:
                  type: string
        '401': 
          description: Unauthorized